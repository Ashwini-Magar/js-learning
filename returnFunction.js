function sum(num1,num2) {
    let add=num1+num2;
return add;
    // or we can only write==>   return num1+num2;

}
let res=sum(10,20);
console.log(`sum is ${res}`);
let res1=sum(20,40);
console.log(`sum is ${res1}`);